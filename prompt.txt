You are a Python code generator. 
Task: Write a parser for PDF tables.

Input:
- PDF file path: {pdf_path}
- PDF sample content: {pdf_data}
- Output CSV path: {output_csv_path}
{f"- Previous error: {error}" if error else ""}

Requirements:
- Output only valid Python code (no explanations or comments).
- Use the camelot library strictly to extract tables.
- Combine extracted tables into a single pandas DataFrame named parsed_df.ames from Skip column names from second page of pdf.
- Ensure numeric columns are safely converted using pandas (handle commas, blanks, invalid values).
- Save parsed_df to CSV at {output_csv_path}.
- Ensure the error given is also focused while writing the code.
- The code must run without modification and without errors.